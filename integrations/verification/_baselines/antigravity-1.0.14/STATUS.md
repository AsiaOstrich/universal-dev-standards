# Antigravity CLI 1.0.14 — experiment state

**Status**: ▶ **active, no quota constraint observed** · **CLI**: `agy` at `/opt/homebrew/bin/agy`
**A separate experiment from Codex.** Findings here do not transfer there, or back.

---

## Why separate

Different tool, different models, different loading mechanism. A survival rate measured on
Antigravity says nothing about Codex and vice versa — the whole point of running both is that
they can disagree. Keeping one table per tool is what makes a disagreement visible instead of
being averaged away.

Antigravity is also the only vehicle here that can vary the **model** while holding the tool
constant: `agy --model` offers 11 models across Gemini, Claude and GPT-OSS. That isolates model
effects from tool effects, which no single-model tool can do.

## What is banked here

| Experiment | Coverage | Status | Location |
|---|---|---|---|
| P2 / P3 baselines | 1 model | ✅ conclusive | `P2.txt`, `P3-CUT.txt` |
| Discarded P1 run (wrong repo) | 1 | 🗄 kept as a warning | `P1-INVALID-wrong-project.txt` |
| P1 baseline, corrected | 1 model | ✅ conclusive (probe cut) | `P1-CUT.txt` |
| **Multi-model form sweep** | **5 models × 2 probes** | ✅ **valid** | `round1-multimodel/` |
| **Outcome measurement, cross-judged** | **2 judges × 2 arms × n=5** | ✅ **complete** | `outcome-cross-judged/` |
| Hard-subject run | — | see directory | `hard-subject/` |

**Result so far**: 10/10 valid cells, every declared-form probe survives unaided across Gemini
3.6 Flash (Low/High), Gemini 3.1 Pro, Claude Sonnet 4.6 and GPT-OSS 120B. Claude Opus 4.6 is
recorded as *not obtained* — simple prompts return, real probes returned empty at 540s with no
error, cause unknown.

## Outcome measurement — done, and it went the other way

| judge | no UDS | UDS | delta |
|---|---:|---:|---:|
| Gemini 3.1 Pro | 6.40 (n=5) | 5.80 (n=5) | **−0.60** |
| Claude Sonnet 4.6 | 6.20 (n=5) | 5.80 (n=5) | **−0.40** |

Both judges negative, ranges overlapping — **not** evidence of harm, but squarely not the Codex
result (+1.15 / +0.93 under the same two judges). Full data and analysis in
`outcome-cross-judged/`.

The unaided baselines differ by ~2.5 defects out of 8 between the two tools, which is the whole
argument for one table per tool: averaging them would produce a number describing neither.

---

## 🔴 This section used to say the opposite, and was wrong for 32 days

Until 2026-08-25 the heading above read **"What has NOT been done here"** and stated that
outcome measurement *"has only been run against Codex"*.

Git says otherwise:

| file | last changed |
|---|---|
| this `STATUS.md` | 2026-07-24 00:34:46 (`f5783757`) |
| `outcome-cross-judged/README.md` | 2026-07-24 00:52:07 (`1da794e1`) |

**Eighteen minutes.** The experiment ran, was committed, and the file whose entire job is to say
what has and has not been done was never updated. On 2026-08-25 that sentence came within one
step of causing a completed experiment to be run again.

This is the shape XSPEC-392 is about — a summary that stopped being true with nothing checking
it — with one difference that matters: **there is no stamp here at all.** A date that lags its
content can be caught by comparing it to git. A sentence asserting the *absence* of work carries
no date, so no clock-based check can see it go stale.

> ⚠️ The sentence above was rewritten once. Its first draft used the same denial phrasing this
> file is about, sitting beside the very directory name it was discussing — and the new gate
> flagged it. **That is the gate working, and it is also the standing cost of checking prose:
> writing *about* a pattern can trip a checker for that pattern.** The fix was to reword, not to
> add an escape hatch; an escape hatch would be used by the next real denial too.

What can see it: **the claim contradicts the directory listing next to it.** `outcome-cross-judged/`
was sitting in the same folder the whole time. That is now enforced by
`scripts/check-baseline-status-claims.ts` — a "not been done" claim about an experiment whose
directory exists is a contradiction, and contradictions are machine-checkable even when
staleness is not.

## Also serving as judge

Antigravity models judged the Codex outcome runs (Gemini 3.1 Pro and Claude Sonnet 4.6). That
is legitimate — the judge is not the tool under test.

For Antigravity's own outcome runs the separation was kept differently: the reviews were
generated by **Gemini 3.6 Flash (High)** and judged by **Gemini 3.1 Pro** and **Claude Sonnet
4.6** — neither judge is the reviewing model. That is a weaker separation than using a
different tool entirely, and it is recorded as such rather than described as equivalent.

⚠️ The line "Codex is currently quota-blocked until 2026-08-22" is also expired: measured on
2026-08-25, `codex-cli 0.145.0` returns exit 0. **A blocker written with a date should be
re-checked when that date passes, not carried forward as if still true.**
